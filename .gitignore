# Secrets — NEVER commit these
.env
.env.local
gmail_credentials.json
gmail_token.json

# Editor backup files (nano, vim, etc.) — these can silently contain secrets
*.save
*.swp
*.swo
*.bak
*~

# Generated content (personal inbox data, not for sharing)
site/

# Python
__pycache__/
*.pyc
.venv/
venv/

# macOS
.DS_Store
